interface:
  display_name: "Project Memory"
  short_description: "Durable Markdown session memory for project work"
  default_prompt: >-
    Use $project-memory to read recent project memory before substantial work
    and write concise session memory afterward.

policy:
  allow_implicit_invocation: true
