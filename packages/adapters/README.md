# Local Install Adapters

Adapters install retrieved skill bundles into configured local agent skill directories.

The server should not mutate arbitrary remote profiles. Installs must be local, configured, path-checked, and auditable.
